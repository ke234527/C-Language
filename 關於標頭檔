C語言無字串之標頭檔
------------------------------------------------
#include<stdio.h>   //是C語言為輸入輸出提供的標準檔

  printf() // 輸出
  scanf()  // 輸入 
  
  
-------------------------------------------------
#include<stdlib.h>  

  system() // e.g.system("pause")是呼叫DOS中的pause指令，避免DOS在執行指令後就直接關閉
-------------------------------------------------
#include<math.h>   //提供用於常用高級數學運算的運算函數

  sqrt() // 平方根

